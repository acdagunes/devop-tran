### Terraform Installation (Binary Method)

1. Installed wget and unzip: `sudo dnf install -y wget unzip`
2. Downloaded version 1.8.0: `wget ...`
3. Moved binary to PATH: `sudo mv terraform /usr/local/bin/`
